{
  "name": "command-launcher-tree",
  "displayName": "Command launcher",
  "description": "Run command with different types of arguments.",
  "version": "0.0.3",
  "license": "MIT",
  "publisher": "Agui",
  "repository": {
    "type": "github",
    "url": "https://github.com/Anthony-Guillaume/CommandLauncher"
  },
  "engines": {
    "vscode": "^1.72.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onView:command-launcher"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "commandLauncher.refresh",
        "title": "Refresh",
        "icon": "$(refresh)"
      },
      {
        "command": "commandLauncher.run",
        "title": "Run",
        "icon": "$(run)"
      },
      {
        "command": "commandLauncher.runLast",
        "title": "Run last",
        "icon": "$(refresh)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "command-launcher",
          "title": "Command launcher",
          "icon": "$(run)"
        }
      ]
    },
    "views": {
      "command-launcher": [
        {
          "id": "launcher",
          "name": "At your command",
          "contextualTitle": "Command launcher"
        }
      ]
    },
    "menus": {
      "view/title": [
        {
          "command": "commandLauncher.refresh",
          "when": "view == launcher",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "commandLauncher.run",
          "when": "view == launcher && viewItem == hasCommand",
          "group": "inline"
        },
        {
          "command": "commandLauncher.runLast",
          "when": "view == launcher && viewItem == hasCommand",
          "group": "inline"
        }
      ]
    },
    "configuration": {
      "title": "Actions",
      "properties": {
        "commandLauncher.actions": {
          "type": "array",
          "description": "Array of actions to be provided to user.",
          "default": [],
          "items": {
            "type": "object",
            "description": "Command and variables associated.",
            "additionalProperties": false,
            "required": [
              "command",
              "variables"
            ],
            "properties": {
              "cwd": {
                "description": "The current working directory.",
                "type": "string",
                "minLength": 1
              },
              "group": {
                "description": "All commands with the same group will be packed in the tree.",
                "type": "string",
                "default": ""
              },
              "label": {
                "description": "Label to be shown in tree. If none, command will be shown instead.",
                "type": "string",
                "default": ""
              },
              "preCommand": {
                "description": "Command that will be launched only at terminal creation.",
                "type": "string",
                "default": ""
              },
              "command": {
                "description": "Command to be executed.",
                "type": "string"
              },
              "variables": {
                "type": "object",
                "description": "Variables to be substituted in the command",
                "additionalProperties": {
                  "type": "object",
                  "properties": {
                    "options": {
                      "type": "array",
                      "items": {
                        "type": "string"
                      }
                    },
                    "placeholder": {
                      "type": "string"
                    }
                  },
                  "required": ["placeholder"]
                }
              },
              "revealConsole": {
                "description": "Whether to reveal the terminal console when running the command.",
                "type": "boolean",
                "default": true
              }
            }
          }
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "webpack",
    "watch": "webpack --watch",
    "package": "webpack --mode production --devtool hidden-source-map",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/glob": "^8.0.0",
    "@types/mocha": "^10.0.0",
    "@types/node": "16.x",
    "@types/vscode": "^1.72.0",
    "@typescript-eslint/eslint-plugin": "^5.38.1",
    "@typescript-eslint/parser": "^5.38.1",
    "@vscode/test-electron": "^2.1.5",
    "eslint": "^8.24.0",
    "glob": "^8.0.3",
    "mocha": "^10.0.0",
    "ts-loader": "^9.4.1",
    "typescript": "^4.8.4",
    "webpack": "^5.74.0",
    "webpack-cli": "^4.10.0"
  }
}
